<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.renjithis.xposed.mods.ussdfilter"
    android:versionCode="1"
    android:versionName="0.2" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data android:name="xposedmodule" android:value="true"/>
        <meta-data android:name="xposedminversion" android:value="2.1*"/>
        <meta-data android:value="Filter out popups shown for call/sms/data usage/balance. Create file with name USSDFilterString.conf in the root of external (if availalbe) or /sdcard of internal storage" android:name="xposeddescription"/>
    </application>

</manifest>